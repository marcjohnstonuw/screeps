var utils = require('utils');
module.exports = function (phalanxInfo) {
	for (var i = 0; i < phalanxInfo.healers; i++) {
		phalanxInfo.healers[i].moveTo(phalanxInfo.currentPosition)
	}
	for (var i = 0; i < phalanxInfo.archers; i++) {
		switch(i) {
			case 0;
				phalanxInfo.
		}
	}
}